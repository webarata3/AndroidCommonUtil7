language: android

jdk:
  - oraclejdk8

android:
  components:
#    - platform-tools
#    - tools
    # ビルドツールバージョン
    - build-tools-25.0.0
    - extra-android-support
    # Android SDKバージョン
    - android-25
  licenses:
    - 'android-sdk-license-.+'

script:
    - ./gradlew test --stacktrace

after_success:
    - ./gradlew jacoco coveralls
